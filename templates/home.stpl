<!DOCTYPE HTML>
<html lang="en">
	<head>
		<title>Russet - Home</title>
		<meta charset="utf-8" />
		<link rel="stylesheet" href="styles.css" />
		<style>
			.alt {
				background: #333;
			}
			.unread {
				font-weight: bold;
			}
			td {
				padding-left: 1em;
				padding-right: 1em;
				border: none;
			}
			td.center {
				text-align: center;
			}
		</style>
	</head>
	<body>
		<p>User: <%= user.name %></p>
		<h1>Entries:</h1>
		<form><table>
			<thead class="alt"><tr>
				<th scope="col"><input type="checkbox" name="select-all" /></th>
				<th scope="col">Title</th>
				<th scope="col">Date</th>
				<th scope="col">Feed</th>
			</tr></thead>
			<tbody>
<% for (i, entry) in entries.iter().enumerate() { %>
<% let mut classes = vec![]; %>
<% if i % 2 == 1 { classes.push("alt") }; %>
<% if !entry.read { classes.push("unread") }; %>
<% let classes = if classes.len() > 0 {
	format!(" class=\"{}\"", classes.join(" "))
} else {
	"".to_string()
}; %>
				<tr<%- classes %>>
					<td class="center"><input type="checkbox" name="select-<%= entry.id %>" /></td>
					<td><a href="entry/<%= entry.id %>"><%= entry.title %></a></td>
					<td class="center"><%= entry.article_date %></td>
					<td class="center"><%= "some feed idk" %></td>
				</tr>
				<% } %>
			</tbody>
		</table></form>
		<div>Page:
			<% if page_num > 1 { %><a href="?page_num=0">1</a>…<% } %>
			<% if page_num > 0 { %><a href="?page_num=<%- page_num - 1 %>"><%- page_num %></a><% } %>
			<%- page_num + 1 %>
			<a href="?page_num=<%- page_num + 1 %>"><%- page_num + 2 %></a>
		</div>
		<div><a href="/subscribe">Subscribe</a></div>
	</body>
</html>
